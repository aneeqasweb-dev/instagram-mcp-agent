{
  "name": "@instagram-agent/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@instagram-agent/agent-core": "0.1.0",
    "@instagram-agent/contracts": "0.1.0",
    "@instagram-agent/mcp-client": "0.1.0",
    "@instagram-agent/mongodb-adapter": "0.1.0",
    "@instagram-agent/ollama-adapter": "0.1.0",
    "fastify": "^5.0.0",
    "mongodb": "7.5.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0"
  }
}
